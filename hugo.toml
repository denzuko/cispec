baseURL = "https://cispec.org/"
languageCode = "en-GB"
title = "cispec"

enableGitInfo = true
disableKinds  = ["taxonomy", "term", "rss"]

[params]
  description   = "org.cispec — a domain-scoped identifier namespace for attesting Change Items across IT, OT, and service delivery."
  organisation  = "Da Planet Security"
  pen           = "42387"
  duns          = "039-271-257"
  specVersion   = "1.0"
  licenceSPDX   = "BSD-2-Clause"
  repoCanonical = "https://github.com/denzuko/cispec"
  predecessor   = "net.matrix"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    style     = "monokai"
    noClasses = false
